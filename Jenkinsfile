pipeline{
    agent any
    stages {
        stage('build'){
            steps{
                echo "BRANCHE_NAME: ${env.BRANCHE_NAME}"
                echo "BRANCHE_IS_PRIMARY: ${env.BRANCHE_IS_PRIMARY}"
                echo "CI: ${env.CI}"
                echo "BUILD_NUMBER:RANCHE_NAME: ${env.BUILD_NUMBER}"
                echo "JENKINS_URL: ${env.JENKINS_URL}"
            }
        }
        
    }

}