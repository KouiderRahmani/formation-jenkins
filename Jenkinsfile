pipeline{
    agent any
    stages {
        stage('build'){
            steps{
                echo "BRANCH_NAME: ${env.BRANCH_NAME}"
                echo "BRANCH_IS_PRIMARY: ${env.BRANCHE_IS_PRIMARY}"
                echo "CI: ${env.CI}"
                echo "BUILD_NUMBER:RANCHE_NAME: ${env.BUILD_NUMBER}"
                echo "JENKINS_URL: ${env.JENKINS_URL}"
            }
        }
        
    }

}